apiVersion: v1
kind: Service
metadata:
  name: tribunal-site
  namespace: zaphod
  labels:
    app: tribunal-site
spec:
  type: ClusterIP
  selector:
    app: tribunal-site
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
