# CosmWasm contracts target wasm32-unknown-unknown for chain deployment.
# Local cargo build / test use the host target.

[alias]
wasm = "build --release --target wasm32-unknown-unknown"
wasm-debug = "build --target wasm32-unknown-unknown"
unit-test = "test --lib"
schema = "run --bin schema"
